# Fancy colors
alias ls="ls --color=auto"
alias grep="grep --color=auto"

# Quick viewing/editing
alias la="ls -lahH"
alias zs="source $ZDOTDIR/.zshrc"
alias zv="vim $ZDOTDIR/.zshrc"

alias :q="exit"

# NVIM
alias vim="nvim"

# WGET CACHE
alias wget="wget --hsts-file=$XDG_CACHE_HOME/wget-hsts"
